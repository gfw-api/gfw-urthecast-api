{
    "id": "#(service.id)",
    "name": "#(service.name)",
    "tags": ["gfw"],
    "urls": [{
        "url": "/urthecast/map-tiles/:renderer/:a/:b/:c",
        "method": "GET",
        "binary": true,
        "endpoints": [{
            "method": "GET",
            "path": "/api/v1/urthecast/map-tiles/:renderer/:a/:b/:c"
        }]
    }, {
        "url": "/urthecast/archive/scenes",
        "method": "GET",
        "endpoints": [{
            "method": "GET",
            "path": "/api/v1/urthecast/archive/scenes"
        }]
    }]
}
